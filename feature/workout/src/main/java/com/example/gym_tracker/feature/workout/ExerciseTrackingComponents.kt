package com.example.gym_tracker.feature.workout

import androidx.compose.material3.*
import androidx.compose.runtime.*
import androidx.compose.ui.Modifier

/**
 * Placeholder for ExerciseTrackingComponents - temporarily commented out for build testing
 */
@Composable
fun RestTimerCard(
    modifier: Modifier = Modifier
) {
    // TODO: Implement rest timer functionality
    Text(
        text = "Rest Timer - Under Development",
        modifier = modifier,
        style = MaterialTheme.typography.bodyMedium
    )
}

@Composable
fun QuickAddSection(
    modifier: Modifier = Modifier
) {
    // TODO: Implement quick add functionality
    Text(
        text = "Quick Add - Under Development",
        modifier = modifier,
        style = MaterialTheme.typography.bodyMedium
    )
}

@Composable
fun SetListHeader(
    modifier: Modifier = Modifier
) {
    // TODO: Implement set list header
    Text(
        text = "Set List Header - Under Development",
        modifier = modifier,
        style = MaterialTheme.typography.bodyMedium
    )
}

@Composable
fun SetItem(
    modifier: Modifier = Modifier
) {
    // TODO: Implement set item
    Text(
        text = "Set Item - Under Development",
        modifier = modifier,
        style = MaterialTheme.typography.bodyMedium
    )
}

@Composable
fun EmptySetState(
    onAddSet: () -> Unit,
    modifier: Modifier = Modifier
) {
    // TODO: Implement empty set state
    Text(
        text = "Empty Set State - Under Development",
        modifier = modifier,
        style = MaterialTheme.typography.bodyMedium
    )
}